find . -mtime +5 -type fd -delete								[It will delete the files and directories where are not modified more than 5 days]
find . -type f | xargs -0 sed -i '' -e 's/R5/Krishna/g'
find . -type f | xargs grep 'Msg'								[It will look for Msg in all the files]
find . -type f | xargs sed -e 's/Msg/Krishna/g'					[It will find Msg and will repace to Krishna]
awk '{print $1}' filename 										 It will print first element
awk '{print $(NF-2)}' filename 									 It will print 2nd last element
[ -d '/var/tmp/' ] && echo "Directory exist"					[To check directory is exist or not]
[ -f '//var/temp/1.txt' ] && echo "File is exist"				[ To check file is exist or not]
#?																[To see last command status]
curl										[To test end point]
wget										[To download the content from the internet]
cat /etc/os-release								[To see about Linux os]
uname -a									[To see the Kernal version]
free -m										[To see the system memory in mega bytes]
top, mpstat, sysstat								[To see the CPU usage]
netstat										[To list the running ports]
ps -ef										[To check running process]
ps -eo pmem,pcpu,vsize,pid,cmd | sort -k 1 -nr | head -5			[To display top 5 process]
sed 's/findstring/replacestring/' filename					[To find and replace a string in a file]
sed '4ikrishnamurthy' filename 							[ it will insert 4th line as krishnamurthy]
sudo find . -type f -size +1M
ls -lAh | grep -v '^d'								[To display files only]
ls -lAh | grep -v '^-'								[To display directories only]
mount -t cifs -o domain=infor,user=ewsuser1,uid=503,gid=503 //usgvwngpmsvm16.infor.com/HMSimages /home/ngpms/ngpms_1.0		[To mount the windows dir]
